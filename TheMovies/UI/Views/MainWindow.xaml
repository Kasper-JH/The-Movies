<!--
    Single Responsibility Principle (SRP):
    Denne fil indeholder KUN UI-definitionen (layout og bindinger).
    Der er INGEN logik i XAML-filen - al logik ligger i ViewModel'en.
    Bindinger forbinder UI-elementer til ViewModel'ens properties og kommandoer.
-->

<Window x:Class="TheMovies.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TheMovies.UI.ViewModels"
        Title="The Movies - Register Movie" 
        Height="450" Width="550"
        WindowStartupLocation="CenterScreen">

    <!-- DataContext sættes i XAML - View'et kender ViewModel'en -->
    <Window.DataContext>
        <vm:RegisterMovieViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Titel -->
        <Label Grid.Row="0" Grid.Column="0" Content="Title:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,5" VerticalAlignment="Center"/>

        <!-- Duration -->
        <Label Grid.Row="1" Grid.Column="0" Content="Duration (min):" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,5" VerticalAlignment="Center"/>

        <!-- Genre -->
        <Label Grid.Row="2" Grid.Column="0" Content="Genre:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,5" VerticalAlignment="Center"/>

        <!-- Register-knap -->
        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="Register Movie" 
                Command="{Binding RegisterMovieCommand}"
                Margin="0,15" Height="30" HorizontalAlignment="Center" Width="150"/>

        <!-- Navigation: Forrige / Næste -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10">
            <Button Content="◀ Previous" 
                    Command="{Binding PreviousMovieCommand}"
                    Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Next ▶" 
                    Command="{Binding NextMovieCommand}"
                    Width="100" Height="30" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Status Message -->
        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Text="{Binding StatusMessage}" 
                   HorizontalAlignment="Center" FontWeight="Bold" FontSize="14"
                   Margin="0,10"/>
    </Grid>
</Window>